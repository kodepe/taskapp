import {StyleSheet} from 'react-native';

export const SearchBarStyles = StyleSheet.create({
  container: {
    width: '100%',
    flex: 1,
    backgroundColor: '#fff',
    borderColor: '#777',
    borderBottomWidth: 1,
    borderRadius: 4,
  },
});
