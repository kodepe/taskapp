import {StyleSheet} from 'react-native';

export const InputStyles = StyleSheet.create({
  container: {
    width: '100%',
    height: 45,
    maxHeight: 50,
    flex: 1,
    backgroundColor: '#fff',
    borderColor: '#777',
    borderBottomWidth: 1,
    borderRadius: 4,
  },
});
